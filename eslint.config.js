// Minimal ESM ESLint config placeholder
// This avoids importing packages that aren't installed (like @eslint/js).
// The canonical configuration is provided in `eslint.config.cjs` (CommonJS).
export default [];
